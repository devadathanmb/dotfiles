[user]
	email = devadathanmb@gmail.com
	name = Devadathan M B

[alias]
    graph = log --all --graph --decorate
    cm = commit -m
    pom = push origin main
    po = push origin

[safe]
	directory = /opt/flutter
[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
